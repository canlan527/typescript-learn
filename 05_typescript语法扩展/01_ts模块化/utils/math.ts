export function sum(a: number, b: number):number {
  return a+b
}

export type PersonType = {
  name: string,
  age: number
}

export interface IPerson {
  name: string,
  age: number,
  height: number
}